import 'dart:ui';

class Palette {
  static const primary = Color(0xff2C3F70); // bg for intro, some btns and unused nav btns
  static const surfaceApp = Color(0xffE2B2AF);
  static const navIcon = Color(0xffA93F39);
  static const text = Color(0xff222126);
  static const secondary = Color(0xffEBEBED);
  static const button = Color(0xff3A5495);
}